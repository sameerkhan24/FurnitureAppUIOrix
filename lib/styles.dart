import 'package:flutter/material.dart';

Shadow textshadow = Shadow(
                blurRadius: 30.0,
                color: Colors.black.withOpacity(0.4),
                offset: Offset(0, 0),
                );